server:
  port: 8081
  servlet:
    context-path: /

spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  datasource:
    url: jdbc:mysql://localhost:3306/test
    username:
    password:
    driver-class-name: com.mysql.jdbc.Driver
